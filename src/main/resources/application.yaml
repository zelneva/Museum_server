spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/museum
    username: anastasia
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
  redis:
    host: localhost
    port: 6379
#    flush-mode: on-save